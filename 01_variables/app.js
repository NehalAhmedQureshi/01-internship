{
   // * without variable type variable is same as "var".
   //  * var variable can be redecleared in all scopes.
   // * let variable can be redecleared but find in its scope not every where.
   // * const variables can not be redecleared and find in its scope not every where.
   user = 'nehal'
}

console.log("🚀 ~ user:", user)